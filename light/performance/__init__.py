# The value of testArgs is set by bin/perf.py once its arguments have been
# read from the command line.  This is used, for example, by performance
# tests that need to produce filesystem output.
testArgs = None
